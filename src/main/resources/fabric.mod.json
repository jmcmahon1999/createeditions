{
  "schemaVersion": 1,
  "id": "createeditions",
  "version": "${version}",
  "name": "Create Editions",
  "description": "A simple tweak and quality of life mod for Create (Fabric).",
  "authors": [
    "Me!"
  ],
  "contact": {
    "sources": "https://github.com/jmcmahon1999/createeditions"
  },
  "license": "CC0",
  "icon": "assets/createeditions/icon.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "net.ducko.createeditions.CreateEditions"
    ]
  },
  "mixins": [
    "createeditions.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "fabric-api": ">=${fabric_api_version}",
    "create": "${create_version}",
    "minecraft": "${minecraft_version}"
  }
}
